import TodoAdd from './homeSections/TodoAdd';
import TodoList from './homeSections/TodoList';

const HomePage = () => {
	return (
		<>
			<TodoAdd />
			<TodoList />
		</>
	);
};

export default HomePage;
